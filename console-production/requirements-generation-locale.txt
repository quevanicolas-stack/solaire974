# Dépendances de l'adaptateur de génération LOCAL (Stable Diffusion via
# diffusers). Volumineuses (plusieurs centaines de Mo à installer, puis
# plusieurs Go de poids de modèle téléchargés au premier lancement) :
# installées à part, uniquement si tu actives ADAPTATEUR_GENERATION=local_texte
# dans le .env. Le mode "factice" (par défaut) n'en a pas besoin.
#
# Installation : pip install -r requirements-generation-locale.txt

torch>=2.4
diffusers>=0.30
transformers>=4.44
accelerate>=0.33
safetensors>=0.4.3
